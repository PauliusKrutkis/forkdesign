# Cursor CLI agent credentials. You can also run `agent login` instead.
CURSOR_API_KEY=

# Claude agent credentials. Claude Code login is also supported.
ANTHROPIC_API_KEY=

# Optional: path to the Cursor CLI binary when it is not available as `agent`.
CURSOR_AGENT_PATH=
